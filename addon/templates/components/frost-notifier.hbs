{{#each notifier.notifications as |notification index|}}
  {{#frost-notification
    hook=(concat hookQualifiers '-notification')
    hookQualifiers=(extend hookQualifiers index=index)
    notification=notification
  }}
    {{yield}}
  {{/frost-notification}}
{{/each}}
